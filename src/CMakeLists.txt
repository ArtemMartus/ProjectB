function (add_source name)
    cmake_policy (SET CMP0076 NEW)
    target_sources (${LIB_NAME} PRIVATE ${name}.cpp)
    target_sources (${LIB_NAME} PUBLIC  ${INCLUDE_DIR}/${name}.h)
endfunction ()

add_source (Game)
add_source (ViewSide)
add_source (Checkboard)
add_source (Figure)
add_source (Point)
add_source (Saver)
add_source (PathSystem)
add_source (FigureFactory)

cmake_policy (SET CMP0076 NEW)
target_sources (${RUN_NAME} PRIVATE main.cpp)
